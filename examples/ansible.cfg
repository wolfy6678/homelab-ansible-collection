[defaults]
inventory = inventory/hosts.yml
host_key_checking = False
# Prompt once per run for the vault password used by the vault-encrypted
# values in group_vars. Drop --vault-id from the docs if you use a password
# file instead.
vault_identity_list = homelab@prompt

[ssh_connection]
pipelining = True
